{
  "name": "shangab-ssh-sync",
  "version": "1.0.0",
  "description": "GitHub Action to sync a repo folder with a remote server using SSH.",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js -o dist"
  },
  "dependencies": {
    "@actions/core": "^1.10.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.34.0"
  }
}
